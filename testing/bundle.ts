import test from 'ava';
import {AccessTest, getJSONAs, requestAs, testPathAccess, testResStatus} from "./lib/lib";
import {ASSET_1, users, CREATOR_1} from './lib/fixtures'

//Create a new bundle as user
test.serial('Bundle: Create a new bundle as test user', async (t) => {
})


//Create a new bundle as a creator 
//Update bundle to public
//Get a bundle
//Search for a bundle 
//Delete a bundle